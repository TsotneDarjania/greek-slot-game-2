import { CustomSprite } from "../../global/customGameObjects/customSprite";

export class SpinButton extends CustomSprite {
  constructor(aliasKey: string, x: number, y: number) {
    super(aliasKey, x, y);
  }
}
